# API Call

A simple project demonstrating how to fetch data from an API and display it on the webpage.

## Features
- Fetches data from a public API
- Renders the results dynamically in the UI

## Technologies Used
- HTML
- CSS
- JavaScript

## How to Run
1. Clone the repository:
   ```bash
   git clone https://github.com/MohammedGousemohiyuddinYekkaluru/API-Call.git
2. open index.html in your browser
